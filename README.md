# RFIDPipenv
Basic Python project using Pipenv

## Requirements

* Python 3
* Pip
* Pipenv
* Raspberrypi

## Install Pipenv
Run: `pip install --user pipenv`

## Install project

Run : `pipenv install`

## Run project

Run : `pipenv run python RfidReader.py`
